# services/

Supabase 클라이언트, Edge Function 호출 래퍼, 도메인별 API 서비스(property, investment, ai 등)가 위치할 폴더.

STEP 03(Foundation/Scaffold) 범위에는 포함되지 않는다 — Supabase 프로젝트 연결과 Auth는 다음 Phase(Phase 2: Supabase/DB/RLS/Auth)에서 진행한다. API.md의 Edge Function 계약을 기준으로 이 폴더의 서비스 함수들이 작성될 예정이다.
