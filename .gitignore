# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Dependencies
node_modules/

# Env
.env
.env.local

# macOS
.DS_Store

# Logs
npm-debug.*
yarn-debug.*
yarn-error.*

# TypeScript
*.tsbuildinfo

# [STEP: 2026-09-10-3] STEP 00(Git 백업) — 스크래치/백업 파일 제외.
# 앱 코드가 아니라 세션 중 만들어진 임시 백업/이전 설정 스냅샷/작업 원본 자료라
# 저장소에 커밋할 대상이 아니다(특히 zip은 98MB로 저장소를 불필요하게 키운다).
*.before-expo-fix
eslint-effective-config.json
Viets*.zip
작업파일/
supabase/.temp/

# [2026-09-10] Cowork 세션에서 전달받은 파일 사본이 쌓이는 폴더 — 실제 코드는 이미
# 프로젝트의 정상 경로(app/, services/, supabase/migrations/ 등)에 반영되어 있어
# 중복이다.
Claude outputs/

# [2026-09-12] Firebase 서비스 계정 키 — 푸시 발송 권한이 담긴 비밀 키다.
# EAS에 업로드해서 쓰고 저장소에는 절대 넣지 않는다. 이름이 제각각이라 넓게 막는다.
# (google-services.json은 클라이언트 설정이라 비밀이 아니고, 빌드에 필요해서 커밋한다.)
*-firebase-adminsdk-*.json
firebase-service-account*.json
service-account*.json
